---
// 魔法属性已并入 Blog 分类，旧入口重定向到 /blog/
// @ts-nocheck —— 纯重定向桩：sitePath 仅在顶层 return 中使用，astro check 会误报 ts(6133)
import { sitePath } from '../../lib/urls';

return Astro.redirect(sitePath('/blog/'), 301);
---
