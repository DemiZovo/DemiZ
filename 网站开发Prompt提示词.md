# 个人网站开发 Prompt 提示词

以下 Prompt 基于个人程序员主页需求整理，可分别交给 AI 编程助手使用，也可以按顺序组合执行，并部署到 GitHub Pages。

## Prompt 1：设计网站内容模型

```text
请为一个程序员个人网站设计并实现清晰、可扩展的内容模型。网站主要包含技术博客，同时包含生活日志、GitHub 项目展示和个人资料。

请使用 Astro Content Collections 管理技术文章和生活日志，并使用 TypeScript Schema 校验 Frontmatter。技术文章至少包含 title、description、published、updated、category、tags、cover、draft、featured、toc 字段；生活日志至少包含 title、description、published、updated、tags、cover、gallery、draft、featured 字段。草稿不得进入生产页面、搜索索引、RSS 或 Sitemap。

请分别规划技术文章、生活日志、项目配置、图片资源和站点配置的目录结构。文章 URL 使用稳定的英文小写短链接，单词用连字符分隔；技术文章使用 /blog/slug/，生活日志使用 /life/slug/。项目数据采用本地配置为主、构建时补充 GitHub 数据的模式，并支持精选、隐藏和手工排序。图片优先使用 WebP，正文图片必须支持替代文字。

请输出：推荐目录结构、完整字段定义、TypeScript 类型或 Schema、示例内容文件、内容读取与筛选规则，以及新增一篇文章或一个项目的操作说明。实现应保持类型安全、便于维护，并为相册、代码片段收藏和学习打卡等未来栏目保留扩展能力，但不要展示尚未完成的入口。
```

## Prompt 2：配置 GitHub Pages 路径与部署

```text
请为一个 Astro Fuwari 静态网站设计并实现可靠的 GitHub Pages 路径与自动部署方案。网站在 WSL2 中开发，需要兼容“用户名.github.io”根路径部署

请集中配置 Astro 的 site、base 和 output，不允许组件各自硬编码仓库名。导航、文章链接、静态图片、字体、CSS、JavaScript、404 页面、RSS、Sitemap、Canonical URL 和 Open Graph 地址都必须正确处理 base 路径。站内 URL 使用小写英文和连字符，并统一保留尾部斜杠；线上输出不得包含 localhost 或本机文件路径。

请创建 GitHub Actions 工作流：检出代码、安装锁定的 Node.js LTS 版本、使用 npm ci 安装依赖、执行类型与内容检查、完成生产构建、上传 dist 并部署到 GitHub Pages。默认分支可以部署生产环境，Pull Request 只检查和构建，不得覆盖线上站点；部署失败时应保留上一版网站。

请输出：部署模式对应的 Astro 配置、GitHub Actions 文件、GitHub 仓库设置步骤、WSL2 本地调试命令、自定义域名与 CNAME 的后续切换方法，以及针对子路径资源 404、详情页刷新、RSS 和 Sitemap 的验证清单。
```

## Prompt 3：接入隐私友好的访问统计

```text
请为部署在 GitHub Pages 的 Astro Fuwari 个人网站设计并实现轻量、隐私友好的访问统计方案。第一版只需要了解页面浏览量、独立访客趋势、热门页面、来源、设备类型、粗略地区和 404 访问情况，不收集姓名、邮箱、表单内容、精确位置、跨站画像或可长期识别个人的信息。

统计功能必须由统一配置控制，至少包含 enabled、provider、siteId、scriptUrl 和 showPublicCount。统计脚本只能在生产环境且 enabled 为 true 时加载，本地开发和 Preview 环境不得发送统计数据；脚本必须异步或延迟加载，加载失败、被广告拦截器阻止或网络不可用时都不能影响页面展示。请考虑浏览器 Do Not Track 设置。

第一版默认不在页面公开显示精确访问量，也不实现点赞、评论或复杂用户画像。请增加 /privacy/ 隐私说明页，准确说明统计服务、收集数据、用途、Cookie 使用情况、数据托管方式、阻止统计的方法和联系方式。若未来增加公开阅读量，接口失败时应隐藏数字区域，不得错误显示为零。

请先比较适用于 GitHub Pages 的无 Cookie 统计方案，再推荐一个最符合静态网站、低维护和中国大陆访问场景的方案。输出应包括选择理由、配置结构、Astro 集成代码、环境判断方式、安全与性能措施、隐私页面草稿，以及验证统计仅在生产环境生效的测试步骤。
```

## Prompt 4：建立验收标准与发布检查

```text
请为一个基于 Astro Fuwari、部署到 GitHub Pages 的程序员个人网站建立可执行、可验证的验收标准，并据此检查实现结果。网站包含首页、技术博客、代码项目、生活日志、关于我、404 和隐私说明页面，支持 Markdown/MDX、中文搜索、标签分类、时间归档、深浅主题、代码高亮、RSS、Sitemap 和响应式布局。

请从以下维度逐项验收：页面和导航、内容模型、草稿隔离、中文搜索、GitHub 项目降级展示、视觉一致性、移动端适配、键盘操作、无障碍、SEO、RSS、性能、统计隐私及 GitHub Pages 自动部署。重点验证 draft 内容不会进入生产页面、搜索、RSS 或 Sitemap；GitHub API 失败不会导致构建失败；320px 宽度无页面级横向滚动；代码块可横向滚动；主题刷新后保持且首次加载无明显闪烁；所有资源在项目子路径部署时不出现 404。

性能验收以生产构建的移动端测试为准：Lighthouse Performance 目标不低于 90，Accessibility、Best Practices 和 SEO 目标不低于 95。允许测试环境造成小幅波动，但不得存在明显布局偏移、超大未压缩图片或阻塞型第三方脚本。

请输出结构化验收清单，每项包含检查方法、预期结果、失败判定和修复方向，并标记哪些项目适合自动化测试、哪些需要人工检查。第一版发布门槛为：至少 3 篇技术文章、2 个项目、1 篇生活日志，真实个人信息已替换，模板示例已清理，手机和桌面端、浅色和深色模式均完成人工检查，并成功完成一次全新的 GitHub Pages 自动部署。
```

## 推荐使用顺序

1. 先使用“设计网站内容模型”，建立目录、类型和示例内容。
2. 再使用“配置 GitHub Pages 路径与部署”，避免后期集中修复路径。
3. 主体页面完成后使用“接入隐私友好的访问统计”。
4. 发布前使用“建立验收标准与发布检查”进行完整检查。
